6
# Baseline established 2026-03-08 with mypy 1.19.1 against src/smart_playlist_io/
#
# Errors by file:
#
# encode.py (5 errors):
#   - _encode_int_rule: value typed as `int | tuple[int, int]` but branches
#     that call _make_int_rule (is/is_not, greater, less) pass it directly as
#     int; mypy can't narrow the union without explicit isinstance guards. [arg-type]
#   - _encode_int_rule: `lo, hi = value` on line 307 fails when value is `int`. [misc]
#   - _encode_date_rule: TIME_UNITS indexed with `unit: str | None`; mypy
#     rejects None as a dict key. [index]
#
# decode.py (1 error):
#   - _decode_children: list append type mismatch for nested subexpr lists. [arg-type]
