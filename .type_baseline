0
# Baseline established 2026-03-08 with mypy 1.19.1 against src/smart_playlist_io/
# Updated 2026-03-09: all 5 errors resolved.
#
# Changes made:
#
# encode.py (4 errors fixed):
#   - _encode_int_rule: added isinstance(value, int) guards before each branch
#     that calls _make_int_rule, allowing mypy to narrow `int | tuple[int, int]`
#     to `int`. [arg-type]
#   - _encode_int_rule: added isinstance(value, tuple) guard before `lo, hi = value`
#     to satisfy mypy that value is iterable in the between branch. [misc]
#
# decode.py (1 error fixed):
#   - _decode_children: annotated `rules` as `list[str | list]` to allow appending
#     nested subexpression lists returned by _decode_subexpr. [arg-type]
